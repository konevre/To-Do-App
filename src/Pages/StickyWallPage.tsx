import React, { memo } from "react";

import StickyWallComponent from "../components/StickyWall/StickyWallComponent";

const StickyWallPage: React.FC = () => {
    return <StickyWallComponent />;
};

export default memo(StickyWallPage);
